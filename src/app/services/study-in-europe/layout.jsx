export const metadata = {
    title: "Study in Europe | Aspire Global Pathways",
    description: "It is agency of Student Counciling",
};

export default function RootLayout({ children }) {
    return (
        <>
            {children}
        </>
    )
}